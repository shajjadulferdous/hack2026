# Optional: enable Gemini-powered case classification and reply drafting.
# When unset, the investigator runs fully offline using deterministic
# rule-based logic (still satisfies the spec).
#remove underscores
# __GEMINI_API_KEY=dibona

# Optional: override the model. Default is gemini-2.5-flash.
# __GEMINI_MODEL=gemini-3.5-flash


